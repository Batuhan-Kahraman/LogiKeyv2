@using EntityLayer.Concrate;
@model List<TasimaModel>

@{
    ViewData["Title"] = "Tasima";
}
<div class="col-lg-10 col-xxl-12">
    <h2 class="mb-3 lh-sm">Taşıma</h2>
    @if (TempData["Msg"] != null)
    {
        <div class="d-flex">
            <div class="toast show align-items-center text-white dark__text-gray-1100 border-0 m-3" role="alert" data-bs-autohide="false" aria-live="assertive" aria-atomic="true" style="background:@TempData["Bgcolor"]">
                <div class="d-flex">
                    <div>
                        @TempData["Msg"]
                    </div>

                </div>
            </div>
        </div>

    }

    <div class="p-4 code-to-copy">
        <div id="tableExample3" data-list='{"valueNames":["urun","plaka","surucu","odeme","alici","gonderici"],"page":20,"pagination":true}'>
            <div>
                <a asp-controller="Tasima" asp-action="TasimaEkle" class="button-style">Taşıma Ekle</a>
            </div>
<!--
            <div class="table-responsive">
                <table class="table table-striped table-sm fs-9 mb-0">
                    <thead>
                        <tr>
                            <th class="sort border-top border-translucent ps-3" data-sort="urun">Taşınan Ürün</th>
                            <th class="sort border-top border-translucent ps-3" data-sort="plaka">Araç Plaka</th>
                            <th class="sort border-top border-translucent ps-3" data-sort="surucu">Sürücü</th>

                            <th class="sort border-top border-translucent ps-3" data-sort="odeme">Ödeme Yapan</th>
                            <th class="sort border-top border-translucent ps-3" data-sort="alici">Alıcı</th>
                            <th class="sort border-top border-translucent ps-3" data-sort="gonderici">Gönderici</th>
                            <th class="sort border-top">
                                İşlemler
                            </th>

                        </tr>
                    </thead>
                    <tbody class="list">
                        @foreach (var item in Model)
                        {/*
                            <tr>
                                <td>@item.TasinacakUrun.Adi</td>
                                <td>@item.Arac.Plaka</td>
                                <td>@item.Surucu.Isim @item.Surucu.Soyisim</td>
                                <td>@item.CariOdemeYapan.Cari_Unvan</td>
                                <td>@item.CariAlici.Cari_Unvan</td>
                                <td>@item.CariGonderen.Cari_Unvan</td>

                                <td>
                                    <a href="#" onclick="Duzenle(@item.Tasima.Tasima_ID, '@item.TasinacakUrun.Adi')">
                                        <span class="text-body fs-5" data-feather="edit"></span>
                                    </a>
                                    <a href="#" data-bs-toggle="modal" data-bs-target="#sil" onclick="document.getElementById('silID').value='@item.Tasima.Tasima_ID';"><span class="text-body fs-5" data-feather="trash-2"></span></a>

                                </td>
                            </tr>
                      */  }
                    </tbody>
                </table>

                -->



            </div>
            <div class="d-flex justify-content-between mt-3">
                <span class="d-none d-sm-inline-block" data-list-info="data-list-info"></span>
                <div class="d-flex">
                    <button class="page-link" data-list-pagination="prev"><span class="fas fa-chevron-left"></span></button>
                    <ul class="mb-0 pagination"></ul><button class="page-link pe-0" data-list-pagination="next"><span class="fas fa-chevron-right"></span></button>
                </div>
            </div>
        </div>
    </div>

</div>
<style>
    .button-style {
        display: inline-block;
        padding: 10px 20px;
        background-color: #007bff; /* Buton arka plan rengi */
        color: #fff; /* Buton metin rengi */
        text-decoration: none;
        border-radius: 5px;
        border: none;
        cursor: pointer;
        margin-bottom:30px;
    }

        .button-style:hover {
            background-color: #0056b3; /* Butonun üzerine gelindiğindeki arka plan rengi */
        }

</style>